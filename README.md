# Language_Detection_Model
This project is designed to detect multiple languages present in an image. It uses Optical Character Recognition and Language Detection Model. When an image gets uploaded it detects the languages and ranks the language based on which language the web page/image contains the most.
